이 케이스는 deterministic static-axis drill이다. 모델 turn 없이 fake Codex/Claude
runtime으로 전체 launch chain을 실행해 bounded N-way(실제 3-way 포함) leg의 시간
중첩, Fleet의 동시 `working` 관측, profile/perspective 비대칭성, 원자 승인, 개별
우회 차단, manifest 결속과 lease 회수를 검사한다.
