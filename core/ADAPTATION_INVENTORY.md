# Adaptation Inventory

This is the working inventory for turning the historical Claude Code setting
into a portable agent setting plus runtime adapters.

## Status Classes

| Status | Meaning |
|---|---|
| `portable` | Can be consumed by multiple runtimes as-is. |
| `adapter-native` | Belongs to one runtime adapter. |
| `projection` | Versioned mirror into a runtime home; symlink/generated only. |
| `compat-reference` | Historical copy retained only for parity/drift checks after adapter-native realization exists. |
| `compat-passthrough` | Still shared because Claude Code currently consumes it directly. |
| `needs-split` | Must be separated into portable source plus adapter-native realization. |

## Current Surfaces

| Surface | Current location | Status | Target split |
|---|---|---|---|
| Core operating contract | `core/CORE.md`, `core/WORKFLOW.md`, `core/CONVENTIONS.md`, `core/OPERATIONS.md`, `core/MEMORY.md`, `core/DESIGN_PRINCIPLES.md` | portable | Keep concrete runtime names out unless documenting adapter mapping. Adapter behavior changes start here, then derive outward. |
| Adapter boundary | `core/ADAPTATION.md`, `adapters/*/ADAPTATION.md` | portable / adapter-native | Single source for what symlink means and what still needs adaptation. |
| Claude bootstrap | `adapters/claude/CLAUDE.md` | adapter-native | Keep Claude Code response, routing, hook, statusline, and command rules here. |
| Claude runtime workers | `adapters/claude/bin/*.sh` | adapter-native | Own concrete Claude CLI worker invocations used by shared dispatchers. |
| Codex bootstrap | `adapters/codex/AGENTS.md` | adapter-native | Expand only with behavior that Codex can actually perform. |
| Codex preflight wrappers | `adapters/codex/bin/preflight.sh`, `role-map.sh`, `capability-map.sh`, `mode-map.sh`, `distill-worker.sh` | adapter-native | Executable Codex bridge for hook invariants, prompt-signal routing context, portable role mapping, capability realization, mode support classification, and distill proposals. Codex hook commands call back into this wrapper where lifecycle hooks exist. |
| OpenCode bootstrap | `adapters/opencode/AGENTS.md` | adapter-native | Loaded through the `instructions` array in `opencode.json`/`opencode.jsonc`; expand only with behavior that OpenCode can actually perform. |
| OpenCode preflight wrappers | `adapters/opencode/bin/preflight.sh`, `role-map.sh`, `capability-map.sh`, `mode-map.sh`, `distill-worker.sh` | adapter-native | Executable OpenCode bridge for hook invariants, prompt-signal routing context, portable role mapping, capability realization, mode support classification, `opencode export` distill delta extraction, and the no-tools auto-distillation worker plus `session-end` trigger. OpenCode plugin hooks call back into this wrapper for guard enforcement and `session.idle` auto-distillation. |
| Codex native skill surface | `adapters/codex/skills/*/SKILL.md` | adapter-native generated projection | Generated from `capabilities/*.md` by `adapters/codex/bin/sync-native-skills.py`; projected as `codex_setting/codex-skills`. Runtime discovery is either native symlinks under `$CODEX_HOME/skills/` or the installable `hearting-codex` plugin, not both by default. Do not reuse Claude Skill files as Codex-native output. |
| Codex native agent surface | `adapters/codex/agents/*.toml` | adapter-native generated projection | Generated from `roles/README.md` role profiles, plus an explicit `EXTRA_AGENTS` out-of-catalog path in `adapters/codex/bin/sync-native-agents.py` (e.g. `memory-scout`, sourced from `core/MEMORY.md` §7.4 rather than a role-catalog row); projected as `codex_setting/codex-agents`. Install by symlinking generated TOML files into `$CODEX_HOME/agents/` or project `.codex/agents/`. Do not reuse Claude Agent files as Codex-native output. |
| Codex native mode surface | `adapters/codex/modes/*/*.md` | adapter-native generated projection | Generated from the unit catalog `roles/units/*/*.md` (unit-bearing manifest modes only) by `adapters/codex/bin/sync-native-modes.py`; projected as `codex_setting/codex-modes`. `mode-info` reports the native guide path plus tool-contract metadata. Do not reuse Claude mode projection files as Codex-native output. |
| Codex native plugin surface | `adapters/codex/plugins/hearting-codex`, `adapters/codex/plugin-marketplace/.agents/plugins/marketplace.json` | adapter-native generated projection | Generated by `adapters/codex/bin/sync-native-plugin.py` from Codex-native skill output; projected as `codex_setting/codex-plugin-marketplace`. Install with `codex plugin marketplace add` and `codex plugin add`. Do not reuse Claude Skill files as Codex-native output. |
| Codex native hook surface | `adapters/codex/hooks/hooks.json`, `adapters/codex/hooks/sessionstart-lifecycle.py`, `adapters/codex/hooks/sessionend-lifecycle.py`, `adapters/codex/hooks/stop-lifecycle.py`, `adapters/codex/hooks/userprompt-lifecycle.py`, `adapters/codex/hooks/permissionrequest-lifecycle.py`, `adapters/codex/hooks/posttooluse-interaction-clear.py`, `adapters/codex/hooks/pretooluse-write-guard.py`, `adapters/codex/hooks/posttooluse-read-marker.py`, `adapters/codex/hooks/posttooluse-design-check.py`, `adapters/codex/hooks/worker-state-compact.py`, `adapters/codex/hooks/run-hook.sh`, `preflight.sh prompt-signal` | adapter-native projection | Codex `UserPromptSubmit` runs the bounded capsule candidate bridge plus lifecycle context; `prompt-signal` remains a worker-startup/manual subcommand carrying the routing contract. Hooks carry no `statusMessage` labels. `PermissionRequest` publishes only privacy-minimal approval-wait metadata and never owns approve/deny; `PostToolUse` clears that exact marker and also routes actual reads/design saves. `Stop` only clears the exact marker and has no completion authority. `PreToolUse` enforces same-turn recall-opportunity plus targeted material/write guards without a wildcard parent park; projected as `codex_setting/codex-hooks`. Runtime checks report `check=hook-trust:review-needed` until `/hooks` trust records exist. Do not reuse Claude hook files or `settings.json` hook payloads as Codex-native output. |
| Runtime hook output contract | `core/HOOKS.md`, adapter hook bridges, portable guards | portable invariant + adapter-native projection | Every adapter must keep native hook stdout compatible with that runtime's hook protocol. Context hooks should emit the runtime's structured context format; side-effect-only hooks should keep stdout empty unless a structured success object is supported. Helper CLI text must not leak into native hook stdout. |
| Codex TUI config fragment | `adapters/codex/config/tui-statusline.toml`, `adapters/codex/bin/apply-tui-config.sh`, `preflight.sh tui-config`, `codex_setting/codex-config` | adapter-native config fragment | Records the harness-recommended `/statusline` footer item order as a mergeable TOML fragment. `preflight.sh tui-config` applies only `[tui].status_line` and `[tui].status_line_use_colors` to runtime-owned `$CODEX_HOME/config.toml` on explicit invocation. Do not project or commit the full `$CODEX_HOME/config.toml`; it is runtime-owned and may contain trust hashes, project trust, plugin state, model defaults, and other user-local state. |
| Codex runtime projection check | `adapters/codex/bin/install-runtime-projection.sh`, `check-runtime-projection.sh`, `preflight.sh runtime-projection`, `preflight.sh doctor --runtime` | adapter-native runtime bridge | Installs and verifies `$CODEX_HOME` links for `hearting`, bootstrap, common docs, capabilities, roles, bin/tools/utilities, scaffolds, hooks, selected native skill discovery (`native|plugin|both`), native agents, native modes, plugin marketplace, and the read-only config fragment pointer without touching Codex credentials, sessions, logs, caches, `config.toml`, or databases. Default `doctor` stays repo/adapter readiness; `doctor --runtime` includes installed runtime wiring. |
| Codex command-like surface | `adapters/codex/skills/*`, `adapters/codex/plugins/hearting-codex` | adapter-native via skills/plugins | Codex custom prompts are deprecated; command-like capability entrypoints are realized through native Skills and the installable plugin. Do not generate `prompts/` or reuse Claude slash-command files as Codex-native output. |
| Codex permission/sandbox contract | `adapters/codex/bin/preflight.sh permissions` | adapter-native wrapper | Reports Codex approval policy and sandbox surfaces; Claude `allowedTools` is unsupported and must not be projected or translated literally. |
| Codex MCP contract | `adapters/codex/bin/preflight.sh mcp` | adapter-native wrapper | Reports Codex native MCP CLI/config surfaces; Claude `settings.json` MCP registrations and wholesale `tools/design-mcp` projection are unsupported. |
| OpenCode native skill surface | `adapters/opencode/skills/*/SKILL.md` | adapter-native generated projection | Generated from `capabilities/*.md` by `adapters/opencode/bin/sync-native-skills.py`; projected as `opencode_setting/opencode-skills`. Do not reuse Claude Skill files or rely on legacy external Skill autoload compatibility paths. |
| OpenCode native agent surface | `adapters/opencode/agents/*/*.md` | adapter-native generated projection | Generated from `roles/README.md` role profiles, plus an explicit `EXTRA_AGENTS` out-of-catalog path in `adapters/opencode/bin/sync-native-agents.py` (e.g. `memory-scout`, sourced from `core/MEMORY.md` §7.4 rather than a role-catalog row); projected as `opencode_setting/opencode-agents`. Install by symlinking generated agent files into `$HOME/.config/opencode/agent/` or project `.opencode/agent/`. Do not reuse Claude Agent files as OpenCode-native output. |
| OpenCode native command surface | `adapters/opencode/commands/*.md` | adapter-native generated projection | Generated from `capabilities/*.md` by `adapters/opencode/bin/sync-native-commands.py`; projected as `opencode_setting/opencode-commands`. Install by symlinking generated command files into `$HOME/.config/opencode/command/` or project `.opencode/command/`. Do not reuse Claude command files as OpenCode-native output. |
| OpenCode native plugin surface | `adapters/opencode/plugins/hearting-guards.js` | adapter-native projection | OpenCode JS transiently captures the current prompt with `chat.message`, runs the bounded capsule candidate probe in `experimental.chat.system.transform`, then discards it. The transform also supplies memory/prompt-signal/briefing context. `tool.execute.before` forwards the native message/turn ID to same-turn recall-opportunity and write guards, `command.execute.before` covers spec capabilities, and `tool.execute.after` covers read markers and design checks; projected as `opencode_setting/opencode-plugins`. Do not reuse Claude hook files as OpenCode-native output. |
| OpenCode permission contract | `adapters/opencode/bin/preflight.sh permissions` | adapter-native wrapper | Reports OpenCode native permission config and plugin surfaces; Claude `allowedTools` is unsupported and must not be projected or translated literally. |
| OpenCode MCP contract | `adapters/opencode/bin/preflight.sh mcp` | adapter-native wrapper | Reports OpenCode native MCP CLI/config surfaces; Claude `settings.json` MCP registrations and wholesale `tools/design-mcp` projection are unsupported. |
| Claude settings/hooks registration | `adapters/claude/settings.json` | adapter-native | Codex must get wrapper/preflight equivalents, not this JSON. |
| Claude slash commands | `adapters/claude/commands/` | adapter-native | Claude Code owns slash-command files. Codex realizes command-like capability entrypoints through native Skills/plugins, and OpenCode realizes them through native command projections. Do not reuse Claude command files outside the Claude adapter. |
| Portable capability catalog | `capabilities/README.md`, `capabilities/*.md` | portable | Per-capability specs define runtime-neutral contracts; Codex and OpenCode resolve entries through adapter-owned `capability-map.sh` wrappers. |
| Claude skills | `adapters/claude/skills/*/SKILL.md` | adapter-native projection, compat-content | Concrete Claude Skill files preserve current Claude behavior while portable contracts grow under `capabilities/`. Root `skills/` is a parity-guarded compat-reference, not a runtime projection. |
| Portable role catalog | `roles/README.md` | portable | Grow into per-role specs when adapter parity work needs finer granularity; Codex and OpenCode generate native agent projections while resolving concrete runtime settings through adapter-owned `role-map.sh` wrappers. |
| Unit catalog doctrine | `roles/MODES.md` | portable | Doctrine layer over the unit catalog `roles/units/` (schema in `roles/units/_schema.md`); Codex and OpenCode resolve units through adapter-owned `mode-map.sh` wrappers, including `tool_contract`, optional `tool_contract_check`, `runtime_surface`, and `fallback` fields for conditional or unsupported units. |
| Claude agents | `adapters/claude/agents/*.md` | adapter-native | Preserve Claude Agent frontmatter/model/tool schema while realizing `roles/README.md`. |
| Unit catalog | `roles/units/*/*.md` (+ `_shared/` fragments) | portable | Single behavior SoT replacing the retired `roles/modes/` and `adapters/claude/agent-modes/` double copy (re-homed 2026-07-22); dispatch reads unit bodies directly, Codex projects native guides from them, guards: `tools/check-unit-config.py`. |
| Hook invariant catalog | `core/HOOKS.md` | portable | Names hook-level invariants and classifies current scripts. |
| Hook scripts | `adapters/claude/hooks/*.sh`, shared `hooks/*.sh` | adapter-native projection, mixed content | Concrete Claude hook files preserve current behavior while splitting invariant checks from runtime hook payload wrappers. |
| Memory distiller | `hooks/mem-distill-dispatch.sh`, `tools/memory/`, `adapters/*/bin/*distill*` | mixed | Keep DB/CLI and dispatcher contract portable; adapter bins own session source and model invocation. Codex's no-tools worker is its `codex exec` read-only sandbox; OpenCode's is `opencode run --pure --agent <distiller>` with every built-in tool disabled (verified: no shell exec, no hang, timeout-guarded). Codex and OpenCode expose `preflight.sh distill-propose` as a no-tools worker tool-contract by default: the explicit, user-facing proposal stays an opt-in preview — Codex exits 69 until `CODEX_DISTILL_ENABLE=1`, and OpenCode exits 69 until `OPENCODE_DISTILL_ENABLE=1`. The adapter-owned `session-end` paths apply distillation automatically by default — Codex on its session-end hook, OpenCode on the plugin `event`/`session.idle` trigger (debounced) — opt out with `CODEX_DISTILL_ENABLE=0` / `OPENCODE_DISTILL_ENABLE=0`. |
| Agent notes root | `<agent-notes-root>` | runtime/continuity state | Portable docs define the layer and required queues; data is not harness source and must not be committed here. Adapter docs own concrete local path realizations. |
| Worklog board app | `<worklog-board-app>` plus `<worklog-board-app>-wt/` worktrees | local app workspace / needs-split if promoted | Treat current code, DB/cache, build output, dispatch logs, env files, and worktrees as external to this harness. If promoted later, split source into a separate app repo or portable tool first. Adapter docs own concrete local path realizations. |
| Worklog status helper | `utilities/agent-worklog-state.sh`, Codex/OpenCode `preflight.sh worklog` | portable helper + adapter wrapper | Read-only inventory of configured notes root and board app; no data migration or mutation. |
| Harness status snapshot | `utilities/harness-status.sh`, Codex/OpenCode `preflight.sh status`, Codex `preflight.sh ui-info` | portable helper + adapter wrapper | Read-only, on-demand/worker-startup snapshot for artifact root, notes/worklog configuration, sibling worktree hints, and git risk — it is a manual/worker-startup lookup, not an automatic per-turn injection. Do not duplicate runtime-native model/context/token/session status surfaces; for Codex, leave `/statusline` and `/title` as native built-in item configuration surfaces and use `preflight.sh status` for harness-specific signals; Codex hooks run silently with no `statusMessage` labels, matching Claude Code's quiet hooks. `preflight.sh ui-info` reports that arbitrary Claude-style live statusline scripts are unsupported and maps autopilot/subagent behavior to Codex-native Skills/plugin and explicit/main-dispatched subagents. |
| Token/context telemetry, accounting, and isolated forecast | `tools/fleet/token_budget.py`, `tools/fleet/token_accounting.py`, `tools/fleet/token_experiment.py`, `utilities/token-budget.py`, `utilities/token-budget-experiment.py`, Codex `preflight.sh token-budget` | portable observer + adapter wrapper + isolated experiment | Keep active context, exact directive bytes, and cumulative raw session counters separate. Codex supports exact-session rollout observation, transition-only hook response, and content-free bounded accounting; `kv|json` are the only accounting diagnostics. Claude mirrors the portable Fleet modules and may invoke the isolated CLI explicitly, but has no automatic Phase 2 accounting hook. OpenCode explicitly defers Phase 2 automatic accounting and the Phase 3 CLI. The production hook never imports or activates `offline-forecast-v1`; dynamic evaluation is deterministic, production-disabled, and cannot adopt. Unknown/degraded/unsupported states preserve the normal pipeline and inject zero bytes. Native runtime budgets are adapter-owned validated opt-ins; never mutate runtime config automatically. |
| Codex subagent delegation contract | `adapters/codex/bin/preflight.sh subagent-info` | adapter-native wrapper | Reports the Codex-native subagent surface, checks the `multi_agent` runtime feature through `codex features list`, and keeps Claude Agent frontmatter unsupported. Use this before claiming subagent delegation parity; native custom agent projection remains under `adapters/codex/agents/` and `$CODEX_HOME/agents`. |
| Headless dispatch contract | Codex/OpenCode `preflight.sh headless` + `preflight.sh dispatch` + `preflight.sh liveness` + `preflight.sh harvest`; Codex `utilities/codex-managed-entry.py` | adapter-native tool-contract | Reports runtime-native headless command templates (`codex exec`, `opencode run`), checks candidate worktrees, builds/registers/optionally starts adapter-native headless commands, and appends the portable `.dispatch/jobs.log` open-job record before launch. Liveness is adapter-owned: Codex maps open jobs to session JSONL `cwd`/mtime, OpenCode maps open jobs to SQLite session activity. Harvest is registry-only status and selected `open` to `done` marking; merge and worktree cleanup remain main/orchestrator responsibilities. A registered Codex headless owner may use its private App Server completion supervisor. An explicitly opted-in new interactive Codex session may instead use the owner-only single-ingress managed gateway: the TUI remains the sole approval owner, a control-only sidecar joins an exact sealed batch and submits one bounded typed receipt, manual/completion races are serialized, and ambiguous accepted sends fail closed. Delivery is selected by parent runtime, so a Claude parent retains Claude resume supervision even for a Codex child. An unmanaged interactive Codex parent fails new detached owner registration before mutation/spawn; only a low-level operator-only escape hatch may select finite `poll-fallback`, and the portable owner selector forbids it. Stop does not join and PreToolUse does not park all tools. This Codex projection is not evidence that sibling adapters share its session surface. Do not reuse Claude headless flags or session paths. |
| Worktree path isolation guard | `hooks/worktree-path-guard.sh` (portable `git worktree add` path check), Claude `settings.json` `EnterWorktree`+`Bash` PreToolUse wiring | portable invariant + adapter-native surface | Enforces the sibling `<repo>-wt/<slug>` convention (OPERATIONS §5.10 ②): deny a `git worktree add` targeting a path outside `<repo>-wt/` (portable, any runtime with a shell `worktree add`), and deny the runtime-native worktree tool whose default lands inside the repo. The built-in-tool deny is **Claude-native**: it binds to Claude Code's `EnterWorktree` tool. Codex and OpenCode expose no EnterWorktree-style tool, so they have no such surface to deny and must not claim the built-in-tool half — they carry only the portable `git worktree add` path check via their shell write/exec guards. Disclose, do not overclaim. |
| Design visual harness | Claude `tools/design-mcp/`, Codex/OpenCode `adapters/*/tools/design/visual-harness.sh` through `preflight.sh visual-harness` | mixed / adapter-native tool-contract | Keep render/check semantics portable. Claude owns the full MCP implementation; Codex/OpenCode expose adapter-owned render/screenshot/console wrappers backed by the shared checker, and report exit 69 only when local Playwright dependencies are unavailable. Do not project Claude MCP files into Codex/OpenCode. |
| Utility scripts | shared `utilities/*`; `adapters/claude/utilities` = whole-layer symlink | portable direct consumption | Claude consumes the shared utility layer through one dir-level symlink (2026-07-22 collapse): new utilities resolve with zero per-file mirror work and no Claude-only utility copies exist. Runtime-local Cairn paths are runtime `settings.json` env (`AGENT_NOTES_ROOT`/`CAIRN_APP`/`CAIRN_WT`); `WORKLOG_BOARD_*` remains a read-only legacy fallback. |
| Codex utilities | `adapters/codex/utilities/*` | adapter-native selective projection | Use Codex-owned wrappers where shared helpers have Claude fallback behavior; symlink only portable utility files Codex actually uses, including token-budget and explicit production-disabled token-budget experiment helpers. Do not expose Claude session helpers such as shared `dispatch-liveness.sh` through `codex_setting/utilities`; Codex dispatch liveness is adapter-owned under `adapters/codex/bin/dispatch-liveness.py`. |
| OpenCode utilities | `adapters/opencode/utilities/*` | adapter-native selective projection | Use OpenCode-owned wrappers where shared helpers have Claude fallback behavior; symlink only portable utility files OpenCode actually uses. Token-budget accounting automation and the isolated experiment CLI remain explicitly deferred. Do not expose Claude session helpers such as shared `dispatch-liveness.sh` through `opencode_setting/utilities`; OpenCode dispatch liveness is adapter-owned under `adapters/opencode/bin/dispatch-liveness.py`. |
| Scaffold assets | `adapters/claude/scaffolds/*`, `adapters/codex/scaffolds/*`, shared `scaffolds/*` | adapter-native projection, mixed content | Concrete Claude scaffold files preserve current behavior. Codex projects shared scaffold HTML through `adapters/codex/scaffolds/` and `codex_setting/scaffolds` with Codex-native docs and no Claude runtime paths. Portable template intent remains available from the shared scaffold layer. |
| Loop helpers | `adapters/claude/loops/*`, shared `loops/*`, Codex/OpenCode `preflight.sh loop-info` | adapter-native projection, mixed content | Concrete Claude loop files preserve current drill/oncall/study behavior. Codex and OpenCode expose adapter-owned `loop-info` support/fallback contracts for oncall/note/study/drill without executing Claude-coupled loop scripts; missing native note/drill automation is reported as unsupported/manual-contract. |
| Tool helpers | `adapters/claude/tools/*`, shared `tools/*` | adapter-native projection, mixed content | Concrete Claude tool files preserve current helper behavior while memory/session/runtime-specific assumptions are split behind adapter or tool plugin boundaries. |
| Entry Skill-layer generator and gate | `tools/sync-entry-skill-layer.py`, `tools/entry-skill-layer.test.py` | portable canonical-only / deferred projection | The generator owns canonical compact-router and Claude-reference synchronization; its deterministic gate validates the canonical and generated trees. Claude/Codex/OpenCode do not runtime-discover either helper, so adapter tool projections are intentionally deferred. |
| Skill conformance lint | `tools/skill-conformance/*`, `adapters/claude/tools/skill-conformance/*` | portable checker + Claude projection | The shared checker enforces Claude `SKILL.md` structure and invocation policy. Claude projects it because sync/drill gates inspect both Claude skill trees; Codex and OpenCode classify it as deferred in their selective tool projections because their native Skills are generated from portable capabilities rather than Claude frontmatter. |
| Codex tools | `adapters/codex/tools/*` | adapter-native selective projection | Use Codex-owned launchers for memory, material browser/data/figure/PDF/image-search contracts, research claim verification, QA verification, and design tools that need runtime-specific home resolution; symlink only portable leaf tools with no runtime-home fallback. Do not expose `build-manifest.py`, `design-mcp`, or `web-bundle` through `codex_setting/`. |
| OpenCode tools | `adapters/opencode/tools/*` | adapter-native selective projection | Use OpenCode-owned launchers for memory, material browser/data/figure/PDF/image-search contracts, research claim verification, QA verification, and design tools that need runtime-specific home resolution; symlink only portable leaf tools with no runtime-home fallback. Do not expose `build-manifest.py`, `design-mcp`, or `web-bundle` through `opencode_setting/`. |
| Projection directories | `claude_setting/`, `codex_setting/`, `opencode_setting/` | projection | Must contain only symlinks or generated adapter output. |

## Migration Order

1. **Role vocabulary first**: replace portable docs' concrete model names with
   `fast reviewer`, `deep reviewer`, `external adversary`, and related roles.
   Adapter docs own concrete model names and CLI-specific choices.
2. **Capability specs second**: keep portable capability meaning in
   `capabilities/`; keep Claude Skill syntax in generated or maintained
   `adapters/claude/skills/<name>/SKILL.md` files. Codex and OpenCode must pass
   through `capability-map.sh`, not `skills/*/SKILL.md`.
3. **Agent profiles third**: keep portable role meaning in `roles/`; keep
   concrete frontmatter/model/tool mapping in adapter-native agent files. Codex
   and OpenCode must pass through `role-map.sh` and `mode-map.sh` for runtime
   decisions.
4. **Hook payloads fourth**: keep invariant semantics in `core/HOOKS.md`, then
   isolate Claude event JSON, statusline JSON, ScheduleWakeup, and MCP
   registration behind adapter-native wrappers. Native hook bridges must also
   own the final stdout protocol for that runtime; do not forward portable
   helper text as hook output when the runtime expects JSON or empty output.
5. **Projection last**: after a surface has an adapter-native realization,
   update `claude_setting/`, `codex_setting/`, or `opencode_setting/` to point
   at that adapter output.

## Acceptance Tests

A surface is not considered adapted until all of the following are true:

- The portable document can be read without knowing Claude Code file formats.
- The Claude adapter still exposes the old runtime path and behavior.
- The Codex adapter either exposes an equivalent behavior or explicitly marks
  the behavior unsupported with fallback instructions.
- The OpenCode adapter either exposes an equivalent behavior using OpenCode
  native surfaces (commands, skills, agents, plugin hooks, permission config)
  or explicitly marks the behavior unsupported with fallback instructions.
- Runtime-facing surfaces are verified at the level the runtime actually
  discovers: a file being present is insufficient when the runtime expects a
  native skill, command, hook, plugin manifest, TOML entry, or built-in surface.
- Concrete model/runtime names appear only in adapter-native files, tests, or
  legacy compatibility notes.
- `claude_setting/`, `codex_setting/`, and `opencode_setting/` remain projections, not independent
  semantic sources.
